# simulation parameters

# physics_tick value: set to 1 to enable custom physics prossecing for armor_stands 
scoreboard players set physics_tick value 1

# bounce value: controls the % of lost speed when bouncing on walls [0 (no bouncing) ... -10 (full elastic)]
scoreboard players set bounce value -5 